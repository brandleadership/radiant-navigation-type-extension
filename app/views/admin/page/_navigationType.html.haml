- if  @page.parent != nil && @page.parent.parent == nil
  <label for="page_base_gallery_id">Navigation Type</label>
  = select :page, :navigationTypeId,  NavigationType.find(:all).collect{|g| [g.name, g.id]}.unshift(['None', nil])